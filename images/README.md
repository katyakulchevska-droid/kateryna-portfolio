# Images

Drop your exported images here:
- `hero.png` — hero portrait photo
- `hobby1.jpg` through `hobby4.jpg` — hobby section photos
- `proj1.jpg` through `proj3.jpg` — project card previews

After adding images, update the `src` attributes in `index.html` to point to `images/filename.jpg` instead of the base64 data URIs.
